env = XCURSOR_SIZE,24
env = XCURSOR_THEME,Catppuccin-Mocha-Dark-Cursors
env = QT_QPA_PLATFORMTHEME,qt6ct
# AMD GPU (default — skip NVIDIA vars)
env = LIBGL_ALWAYS_SOFTWARE,0
$fileManager = {{ if eq .fileManager "nemo" }}nemo{{ else }}yazi --cwd $HOME{{ end }}
